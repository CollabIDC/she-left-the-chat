In `src/pages/Index.tsx`, swap the order of `<CommunityTeaser />` and `<EmailSignup />` so the component sequence becomes:

- HeroCarousel
- LatestStories
- StumbledUponMadrid
- RealGuides
- EmailSignup
- CommunityTeaser
- Footer

No imports, component code, or any other content is changed.