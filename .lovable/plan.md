In `src/pages/Index.tsx`, remove the `CommunityTeaser` component from the homepage by:
1. Removing the import line for `CommunityTeaser`
2. Removing the `<CommunityTeaser />` JSX element from the render order

The `EmailSignup` component stays in its current position. No other files or content are changed. The `CommunityTeaser.tsx` component file itself is preserved.